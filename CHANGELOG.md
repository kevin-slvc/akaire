# 変更履歴

まだ公開リリースはしていない。以下は公開前の内部版数の記録。

## v1.27

- **コピー文とzipの `review.md` だけでは、2周目の `__rvResolved` を安全に更新できなかった**。
  正本の `prompts/apply-review.md` にしか既存タグの統合手順がなく、簡易コピーやreview.mdだけを
  読んだAIはタグを追記し、前回のidsを後の代入で黙って失わせられた。番号でなくidを使うこと、
  未対応idを入れないこと、読み込み行を残すことをコピー文へ足し、既存タグまたは適用済みrevが
  ある2周目だけ「古いタグを消して1つへ統合・idsは和集合・revは新値」をコピー文とreview.mdの
  両方へ出す。初回には統合説明を増やさない
- **`__rvResolved` のタグが重複しても画面上では何も分からなかった**。処理はグローバル変数を
  1回読むだけで、後のタグが前を上書きしてもエラーにならないため、消し込み漏れを人が発見
  できなかった。`applyResolved()` の先頭でインラインscriptを数え、2個以上なら個数・最後の1個
  しか効かない理由・1個へまとめる対処をトーストで警告する。互換性のため処理は止めず、最後の
  タグを使う挙動は変えない（重複fixtureの実ブラウザ回帰テストあり）
- **既存の印の上をドラッグしても、独立した2件目を作れなかった**。mouseupで選択範囲を見る前に
  `mark.rv` のクリック処理がreturnしていたため、ドラッグも既存コメントの追記として開いていた。
  先に選択の有無を確定し、空なら従来どおり既存コメント、範囲があれば新規コメントへ分けた。
  `page.mouse` を段階的に動かし、クリックとドラッグの両方を固定する回帰テストを足した
- **`body` にtransformがあると、枠・切り取り・選択ハイライトがずれた**（`translateY(20px)` の
  fixtureで実測 縦51px・横31px。ずれ幅はtransformの値と一致しない）。矩形は
  `getBoundingClientRect()` を文書座標へ直していた一方、描画レイヤー自体がtransformされたbodyの
  子だったため変換が二重に掛かった。手動チェックリストの未検証欄に残っていて、通常fixtureでは
  発覚しなかった。矩形専用の4レイヤーを `documentElement` 直下へ移し、
  `body { transform: translateY(20px) }` 上で選択・ドラッグ中の切り取り・保存済み枠の実座標を
  比較する `tests/environment.mjs` を足した
- **IndexedDB APIは存在するが `open()` が拒否される経路は未検証だった**。実装には既に
  `request.onerror` / `onblocked` のフォールバックがあり、拒否を再現するとコンソール警告が出て、
  コメント本文とサムネイルはリロード後も残ったため本体は変更していない。この拒否fixtureを
  `tests/environment.mjs` に固定し、未検証欄から検証済みへ移した

## v1.26

- **コメント欄と各ボタンのフォーカス枠がブラウザ既定の青のままだった**。v1.18でアクセント色を
  akaireの赤に固定したのに、フォーカスリングだけ `outline` を指定しておらず、
  入力欄をクリックした瞬間だけ層に青が現れていた。v1.18が潰したはずの
  「同じファイルなのに色が違う＝版ズレか？」の切り分けを、フォーカス時に再発させていた。
  `:focus` / `:focus-visible` にアクセント色の outline を当てる（キーボード操作の
  可視フォーカスは残す）

## v1.25

公開前に Codex と Grok へ独立でレビューさせ、返ってきた18件を実装コードまたは実測で
裏取りしたうえで全部直した。うち2件は他人の環境に影響が残る種類だったので、公開前に潰した。

- **画像を「×」で外しても原寸がIndexedDBに残り続けていた**。外す操作は表示用の配列から
  抜くだけで、`deleteImages()` はコメントごと削除と全消去からしか呼ばれていなかった。
  画面上は消えるので気づけず、消したつもりの画像がブラウザに残り、UIからは触れなくなる。
  保存の時点で、保存前後の画像名を突き合わせて**参照が消えたものだけ**削除する。
  キャンセルで閉じたときは消さない（消してよいのは保存を押したときだけ）
- **zipへ入れる再取得HTMLの一致判定が、文字数の差だけを見ていた**。差が1割以内なら
  中身が全面的に違っても「一致」で通り、読み手が見ていないHTMLがAIへ渡っていた。
  長さの判定は足切りとして残し、正規化テキストの先頭と末尾も突き合わせる。
  **誤って一致とするより、誤って不一致とするほうが安全**（不一致なら表示中のDOMへ落ちるだけ）
- zipの `review.md` に、ページ由来の文字列（タイトル・見出し・引用）を無加工で連結していた。
  改行を空白へ畳み、コードフェンスになる記号を逃がす。改行を潰した時点で、正規の手順に
  見える見出しを差し込む経路は閉じる
- **画像の添付と取り外しに回帰テストを足した**（`tests/images.mjs`）。表示ではなく
  IndexedDBの実体を読み、外して保存したら消えること・キャンセルでは消えないことの
  両方を見る。上の1件目は画面を見ても気づけない型なので、目視の手順書では守れない

文書側は、実装と食い違っていた15件を直した。

- 導入節だけ実行しても画面に何も出ないことを書いていなかった（成功と、スクリプトが
  読めていない状態が同じ見え方をする）
- 「既定ONを外す」手順が層ごと削除する `strip` しか載っておらず、そのとおりにすると
  読み込み行ごと消えて相手がレビューを返せなくなる。属性だけ外すのは手で1語消す作業
- clone直後に `npm install` と `npx playwright install chromium` が要ることを書いていなかった
- `CONTRIBUTING.md` が「自動テストは無い」のままだった。版数の更新箇所も2箇所しか
  書いておらず、静的チェックが見る4箇所と合っていなかった
- `specs/resolved-contract.md` のコードフェンスが閉じておらず、「タグは1つだけ置く」という
  最重要の規約が見本コードの一部に見えていた
- コメントIDの仕様が `"c" + Date.now()` のままで、実装のタブ接尾辞と連番が抜けていた。
  仕様どおり `^c\d+$` で検証すると実IDを弾く
- `prompts/apply-review.md` が予約ID一覧の在り処を `specs/resolved-contract.md` と
  書いていたが、そのファイルに一覧は無い
- 動作環境の「古い環境では画像とzipだけ劣化する」が実態と違った。`Promise` などを
  トップレベルで使うので、無い環境では層ごと起動しない
- コメントの保存先が `location.pathname` だけで決まることを制限として書いた。これは
  直し忘れではなく、ハッシュとクエリに `#rv` / `?rv=1` が乗るための選択
- 切り取りの案内が Option だけだった。実装は `altKey` を見るのでWindows / Linux の Alt でも
  動き、バーの〈枠〉を押してからドラッグする経路もある。両方書いた
- 生成側の雛形が禁止していたのは `window.__rvResolved` だけで、`window.__rv` が抜けていた
- 仕様書のトースト文言・zipのDOM除去リストが実装より古かった（実装が正しい）
- `file://` が1つの領域として扱われるという記述に、Chromiumで実測した事実であることと、
  ブラウザによっては共有されない可能性を添えた
- READMEにサードパーティの節を作り、デジタル庁デザインシステムのトークンを参照している
  ことと、デジタル庁が作成・監修したものではないことをまとめた

## v1.24

- **テストを走らせられるようにした**（`npm test`）。これまでは fixture と手動チェックリストは
  あったが、走らせる仕組みが無く、壊れていないことを確かめる手段が無かった
  - `tests/static-checks.mjs`: ブラウザ不要。ガイドのステップと進行の呼び出しの食い違い、
    版数3箇所のずれ、`rev` の案内が分止まりでないか、予約IDが実装と雛形で揃っているか、
    同梱HTMLが読み込み行を持っているか。**入れているのは実際に一度やらかした型だけ**
    （落ちても誰も直さない検査を増やすと、全体が信用されなくなる）
  - `tests/roundtrip.mjs`: サンプルを実ブラウザで開き、層の表示→文字/枠/切り取りの3種→
    コピー文のID→`__rvResolved` による済み落ち、までを通しで確かめる。合成イベントでなく
    実マウス入力を使う（合成した MouseEvent は pageX/pageY にスクロール量が乗らず、
    実装が正しくても壊れて見えることがある）
- **静的チェックが初回実行で拾った欠陥を直した**。予約IDのうち8個（`rvdir` `rvthread` `rvsel`
  とガイド用5個）が実装にはあるのに `prompts/generate-html.md` の一覧から漏れていた。
  生成されたHTMLがこれらのIDを使うと、レイヤーは衝突を検出して起動を中止する

## v1.23

- **初めて使う人にサンプルで1周させる導線を足した**。`<script>` 1行で層は載るが、
  載ったことと読み手が使えることは別で、往復のうちブラウザ上の操作はAI側からは見えない。
  やり方を知らない読み手に渡した生成物は、コメントが1件も来ないまま終わる
  - `prompts/onboarding.md`: リポジトリを読んだAI向けの手順。本番の生成に入る前に
    サンプルで「赤を入れる→AIへ渡す→直す→済みへ落ちる」を1周させる
  - `examples/sample-review.html`: 練習台。架空の社内メモに欠陥を3つ仕込んであり、
    文字選択・枠・切り取りの3つの指定方法がちょうど1回ずつ要る。`data-rv-default="on"`
    を付けてあるので `#rv` を打たせずに始められる
  - README と `prompts/generate-html.md` から辿れるようにした
  - **聞くのは一度だけ**。AIは前回の会話を覚えていないので、何も見ずに聞くと相手は
    毎回同じ質問をされる。akaire のルート（`prompts/` の親）に置く `.akaire-onboarded` の
    有無を先に見て、あれば練習の話題ごと出さない。手順書を読む場所と同じなので、
    書いた場所と次に見る場所が必ず一致する。**印は人単位で1つ**＝プロジェクトを
いくつ持っていても誘われるのは一度きり。**飛ばされたときも印を残す**（残さないと、断ったのに
    次の会話でまた聞かれる）。人間がこのファイルを消せば練習し直せる
- **`rev` の案内を分止まりから秒まで直した**。コピー・zip の末尾に出るAI向けの指示が
  `r{YYYYMMDDHHMM}` で、規約（`specs/resolved-contract.md`）が求める秒精度と食い違っていた。
  同じ分に2回対応すると2回目の `rev` が1回目と一致し、二重適用の防止に引っかかって
  `ids` すら見られずに無視される（エラーは出ない）。`rv-layer.js` 2箇所・
  `prompts/apply-review.md`・`specs/review-package.md`・README の例を秒までへ揃えた

## v1.22

- **日本語入力の1文字目が壊れていた**。コメント欄が開いてもフォーカスは本文に残り、
  最初の文字キーのkeydownで初めてコメント欄へ移す作りだった。変換は押した瞬間に始まるので、
  その最中にフォーカスが動くと変換が切れ、ローマ字が生のまま落ちる。「て」を打つと
  「t」＋変換中の「え」になる（実機の録画で確認）。開いた時点でフォーカスを入れるようにした
- **v1.1で入れた「Cmd+Cで選んだ文字列をコピーできる」は別の手で保った**。フォーカスを入れると
  本文の選択が解けるため、コメント欄で何も選んでいないときのCmd+Cは引用
  （`range.toString()`＝選んだ文字列そのもの）をクリップボードへ書く。欄の中で選択している
  ときはそちらのコピーを邪魔しない。v1.1の狙いは結果として同じまま
- クリップボードへの書き込みを `writeClipboard()` に1本化した（〈コピー〉ボタンと共用）。
  execCommandの戻り値を見る従来の作りは維持し、両方失敗したときのファイル書き出しは
  〈コピー〉ボタン側にだけ残した
- **ガイドの進行を番号でなく名前で指すようにした**。v1.21で「動かす」を3番目に挿したとき、
  枠・切り取り・コピーの3箇所が古い番号のまま置き去りになっていた（進行が止まる不具合）。
  同時に「動かす」の進行（コメント欄を実際に動かしたら次へ）を配線した

## v1.21

- **初回ガイドに、画面へ手がかりが出ない操作を2つ足した**。既存の5ステップは
  「付ける→渡す」を一周できていたが、知らないと自力で辿り着けない操作が抜けていた
  - 3番目に「コメント欄を動かす」を新設（v1.20で入れた操作。カーソル以外に手がかりが無い）
  - 最後のメッセージに止め方 `#rv-off` と戻し方 `#rv` を足した。これも画面のどこにも出ない
- 画像の添付・追記・zip・`data-rv-default` は入れていない。添付はコメント欄に常時ヒントが
  出ており、追記は往復が始まってから使うもの、zipは別PCへ渡すときの補助、
  `data-rv-default` は読む人ではなく作る人の話でREADMEとプロンプト雛形の担当

## v1.20

- **コメント欄を動かせるようにした**。コメント欄は選んだ範囲の直下に出るため、長い段落や
  画面下寄りの箇所では指摘対象そのものに重なり、前後の文脈を見ながら書けなかった。
  上部の引用文の帯を掴んでドラッグすると動かせる。掴めるのは引用文だけなので、入力欄の
  選択やボタンの操作は従来どおり。画面外へは出ない（上下左右とも8pxで止まる）
- 動かした位置は開いている間だけ有効で、閉じれば既定の配置へ戻す。次のコメントは別の場所に
  付くので、前回動かした位置を覚えていると対象から離れたところへ出てしまう
- 実装中に見つけた不具合を同時に修正: **コメント欄の外でマウスを離すと位置が既定へ戻っていた**。
  ドラッグの終わりがコメント欄の外に落ちると、本文の選択を確定する通常の経路が走って
  描き直されるため。移動中の離しはその経路へ渡さないようにした。あわせて移動状態の後片付けを
  1周遅らせ、ハンドラの登録順に依存しないようにした

## v1.19

- **作る側が既定を決められるようにした**。読み込み行に `data-rv-default="on"` を足すと、
  そのページは開いた人のブラウザに記録が無くても最初から出る。レビューしてほしくて
  zipやメールで渡すHTMLに付ける想定で、付けなければ従来どおり黙って始まる。
  「出す/出さない」を**環境（file:// かどうか）ではなく、そのHTMLを作った人の意図**で
  決める形にした。環境で決めると、読んでもらうだけのHTMLを渡した相手の画面にも出てしまう
- 見る側の判断を三値（`1`=出す / `0`=出さない / 記録なし=未決）にした。以前は「出さない」を
  鍵の削除で表していたため、既定ONのページでは削除が既定への復帰になり `#rv-off` が
  効かなかった。既存のブラウザに入っている `1` はそのまま有効
- 既定ONのページを開いてもフラグは立てない。同じオリジンにある指定なしのページが
  釣られて出ることはない
- **1語は既定では付けない**。生成側プロンプト雛形（`prompts/generate-html.md`）と README に
  判断基準を書いた。付け忘れは `#rv` で回復できるが、付いたまま外部へ出すと相手の画面に
  出てしまい取り消せない、という非対称性を根拠にしている
- `rv-cli status` が「層あり・既定ON」を区別するようにした。公開・納品の前に外し忘れを
  見つけるため。`strip` は属性つきのタグも従来どおり丸ごと外す（往復で完全一致を実測）
- `rv-cli` がHTML以外のファイルを受け取らないようにした。`rv-layer.js` 自身はコメントに
  読み込み行の例を含むため、渡すと `status` が「層あり」と誤答し、`strip` はその例文を削っていた
- `.rvbak` の無い `restore` は素の1行で戻すため `data-rv-default` は復元されない。
  そのことをメッセージに書いた

## v1.18

- **アクセント色がページ側のパレットに乗っ取られていた**。アクセントの解決順が
  `var(--rv-accent, var(--coral, #a90000))` だったため、`--coral` を定義しているページ
  （Claude.com系のトンマナなど）へ載せると、akaire の赤ではなくそのページのコーラルで
  描画されていた。同じファイルなのにページによって色が変わり、版ズレと見分けがつかない。
  ページ側変数を見にいく段を外し、既定を `#a90000` へ固定した（`--rv-accent-hover` も同様に
  `#ce0000` 固定）。**馴染ませたい場合は従来どおりページ側で `--rv-accent` を定義すれば効く**＝
  機能は残し、既定だけを反転した
- 地の色（`--rv-border` / `--rv-text` / `--rv-surface` / `--rv-inverse`）は従来どおり
  ページ側の値を拾う。ここまで固定すると、暗い地のページでバーが読めなくなるため

## v1.17

- **枠と範囲の「中の塗り」だけ、改名前の青緑が残っていた**。v1.15 で配色を赤へ切り替えたとき、
  枠線は `--rv-accent` を通っていたので赤になったが、重ねて敷く半透明の塗りは値が直接書かれて
  いて、`rgba(0,111,131,...)` のまま3箇所が取り残されていた。透明度が7〜18%と薄いので単体では
  気づきにくいが、白い地の上では赤い枠の中が水色に見える。赤 `#a90000` 由来の値へ差し替え、
  あわせて塗りにも `--rv-mark` と同じようにトークンを与えた
  - `#rvhover`（枠モードで要素にホバー中）… `--rv-hover-fill` 既定 `rgba(169,0,0,.08)`
  - `#rvmarks .rvbox`（枠コメントの確定後）… `--rv-box-fill` 既定 `rgba(169,0,0,.06)`
  - `#rvsel .rvselbox`（Option+ドラッグで範囲を描いている最中）… `--rv-select-fill` 既定 `rgba(169,0,0,.15)`

## v1.16

- **追記を削除しても、保存すると復活していた**。別のタブが足した追記を失わないため、保存の直前に
  手元とディスクの追記を併合しているが、これが和集合だったため、消したものがディスク側から戻って
  いた。コメント本体には「このタブが見たIDなのに手元に無い＝削除した」という判定が既にあったので、
  追記にも同じ判定を入れた。他のタブが足した追記（このタブが見ていないもの）は今までどおり入る
- 併せて、保存データの追記に壊れた要素（`null` や文字列）が混じっていても起動が止まらないようにした。
  上の判定を入れる過程で、壊れた要素に触れて `load()` ごと落ちる経路ができていた

## v1.15

- **同じ1分のうちに同じ文面の追記を2回書くと、2件目が黙って消えていた**。追記が1件ずつのIDを
  持たず、保存時の突き合わせが「作成時刻＋本文」しか見られなかったため（作成時刻は分までの精度）。
  新しく作る追記にIDを振った。すでに保存されている追記はIDを持たないままにしてある
  （複数のタブが同じ追記へ別々のIDを振ると、逆に別物として二重に残るため）

## v1.14

初めて触る人が、ボタンの名前と最初の一手で詰まっていた分。

- **「書き出し」を `zip` に変えた**。何が起きるか（zipが1つ落ちてくる）を名前にした。
  hoverの説明も足した。他のボタン名は変えていない
- **初めて開いた人にだけ、5ステップの使い方ガイドを左下に出す**。読ませるのではなく、
  各ステップは対応する操作が実際に起きたときに進む
  （選ぶ→保存→枠→Option+ドラッグで範囲→コピー）。
  「次へ」で飛ばせて、「もう出さない」でその場で終わる。見終えた記録は
  `localStorage` の `rv-layer:guide` にオリジン単位で持つので、別のページを開いても
  同じ案内を最初から見せない。もう一度見たいときは `window.__rv.guide()`
- ガイドの要素（`#rvguide`）は、名前衝突の検知・zipへ書き出すHTMLからの除去・印刷時の
  非表示・レイヤー自身のクリック判定に、既存のUIと同じように登録してある
- Option+ドラッグの範囲選択（切り取り）は前からあったが、どこにも案内が無く見つけられなかった。
  ガイドの4番目と動画に入れた
- 同じ穴が他にもないか、実装から利用者操作を39個数え上げて全部を実ブラウザで1回ずつ確認した。
  37個は想定どおり、2個（保存先のフォルダ選択・保存後のCmd+C）はOS側のダイアログとクリップボードの
  ため自動では確かめられず手元確認に残した。あわせて全機能版の動画 docs/demo-full.mp4（2分40秒）と
  その字幕を追加。撮り直しは `node tools/record-demo.mjs <出力先> full`
- 確認の過程で見つけた既知の挙動2件を「既知の挙動」としてチェックリストに書いた（いずれも未修正）

## v1.13

- **有効化用の `#rv` / `?rv=1` をURLから消すと、対象ページ自身の `history.state` まで
  `null` にしていた**。URLの印だけを消し、ページが持っていたstateはそのまま渡す
- **同じコメントを見ている別タブで追記・済み状態・本文を変えたあと、古いタブが保存すると
  その変更を古い版で上書きしていた**。このタブで触っていない既知コメントは保存直前の版へ
  更新し、両方のタブで触った場合も追記は和集合にして失わない。削除済みコメントは復活させない
- **書きかけのコメントがある状態で本文や別コメントをクリックすると、警告なく閉じて消えていた**。
  Escape・閉じる・本文クリック・コメント切り替え・新規選択を共通の二段警告に通し、
  1回目は閉じず、4秒以内の2回目で破棄する

## v1.12

独立レビューで出た残りの中・低を潰した。いずれも「書いたものが黙って消える／
別のものと入れ替わる」側。

- **書きかけのコメントがEscape1回で消えていた**。1回目は警告を出し、2回目で破棄する
- **書きかけのまま別のページへ移ると消えていた**。書きかけがあるときだけ確認を出す
  （無いときは対象ページに何も足さない）
- **コメントIDが `c` + `Date.now()` だけで、同じミリ秒に2件作ると衝突していた**。
  衝突すると編集・削除・済み判定が別のコメントを巻き込む。タブごとの接尾辞と連番を足した
- **旧キーからの移行が、同名ファイルが複数パスにあると同じコメントを各ページへ
  複製していた**。旧キーごとに最初に取り込んだパスを記録し、他のパスでは読まない
- **再取得したHTMLが表示中の内容と同じか確かめていなかった**。サーバーが時刻や認証で
  中身を変える場合やSPAで、読み手が見ていない内容をAIが直すことになる。
  見えている文字を突き合わせ、1割を超えて違えば表示中のページを採用する。
  `review.md` の取得経路の記述もその3通りに分けた
- **クリップボードのフォールバックが、失敗しても「コピーしました」と出していた**。
  戻り値を見て、駄目なときはテキストファイルとして書き出す
- **`?rv=1` の印を消すときに、ページ内アンカー（`#section-3` 等）まで捨てていた**。
  rvの印だけ消してアンカーは残す

## v1.11

公開前に、外部モデル4本（Codex 2・Grok 2）で独立レビューを回した結果の反映と、
その過程で見つかった段落破損の修正、配色の入れ替え。

- **保存した位置から復元したコメントが、段落を割ることがあったのを直した**。
  引用が段落の末尾で終わると、レンジの終端が次のブロックの先頭へ落ちて
  境界をまたぎ、`<mark>` が `<p>` ごと飲み込んでいた（元の段落は最初の1文字だけになる）。
  リロードしても直らず、実際にドラッグして再現する。終端の位置を「ひとつ前のノードの末尾」へ
  寄せ、包む直前にも境界を確かめる歯止めを入れた
- **既定の配色をデジタル庁デザインシステムのデザイントークンに入れ替えた**
  （`@digital-go-jp/design-tokens` v2.0.1）。主ボタンは cyan-900、hover は cyan-800。
  文字4.5:1・UI部品の境界3:1（WCAG 2.2 AA）を全項目で満たす。
  以前の配色は主ボタンの白文字が3.28で基準を割っていた。色はすべて `--rv-*` の
  CSSカスタムプロパティで上書きできる
- **ポップアップの入力欄が枠からはみ出していたのを直した**。レイヤーが `box-sizing` を
  指定しておらず、対象ページの設定をそのまま受けていた。`border-box` を敷いていない
  ページでは `width:100%` の入力欄が padding と border のぶん18px はみ出す。
  rvの要素だけに閉じて `border-box` を指定した（対象ページ側の指定は変えない）
- README に「使う前に知っておくこと」を追加。対象ページのスクリプトから
  コメントと画像が読める・書き換えられること、`window.__rvResolved` を
  ページ側から設定できることを、設計上の前提として明記した
- `package.json` / `SECURITY.md` / `CONTRIBUTING.md` / issueテンプレートを追加
- READMEにスクリーンショットを追加

- 読めない保存データがあるとき、空の状態で上書きしていたのをやめた。この文書のものとして
  読めない保存済みデータを見つけたら、そのセッションは書き込みを止めて残す
- 同じページを2つのタブで開くと、あとから保存した側がもう一方のコメントを消していた。
  保存の直前に最新を読んで取り込む。このタブで削除した分は復活させない
- 画像を複数まとめて投下すると上限4枚をすり抜けていた。読み込み中の枚数も数に入れる。
  読み込み中にコメントが切り替わった画像が、別のコメントへ混入するのも止めた
- 容量超過で保存を止めたあと、容量が空いても再試行しなかった。毎回試して復帰する
- 保存データの形を検査し、壊れた1件で全体が起動不能にならないよう隔離する
- **`__rvResolved` のタグは1つだけ**であることを規約とプロンプトに明記した。
  以前のプロンプトは「複数タグがあってもよい・順に適用される」と書いていたが、
  実装は変数を1回読むだけなので後のタグが前を上書きし、消し込みが黙って落ちていた
- 推奨する `rev` を分から秒までに変えた。同じ分に2回対応すると2回目が丸ごと無視されていた
- `review.md` に出る「追記」を仕様書へ記載した（実装は出していたが仕様書に無かった）
- 済み消し込みのトーストに残っていた内部の呼び名を中立語へ
- READMEの導入例が、対象HTMLからの相対パスであることを明記した
- 手動チェックリストに、保存の守り・`source/`・追記・`xss.html` の確認を足した

## v1.10

- 位置を見失ったコメントの一覧表示で、引用文と指摘文を `innerHTML` へ連結していたのをやめた。
  引用文には元のページの文字列がそのまま入るため、タグに見える文字列がHTMLとして
  解釈される経路になっていた。DOMを組んで入れる形に変更（回帰テストあり）
- 配布物から個人名・内部の課題番号・特定のAIツール名を取り除いた。コピー文面の宛先は
  「AIへ」で、Claude Code / Codex / Cursor / Gemini CLI のいずれでも同じ文面を使える
- 変更履歴をファイル冒頭のコメントからこのファイルへ移した

## v1.9

- 版数を `RV_VERSION` として実体に持たせ、バーのhover・`window.__rv.version`・
  起動時のconsoleの3箇所に出す。読まれているファイルがどの版か分かるようにするため
- 「保存先」ボタンを常時表示に変えた。`showDirectoryPicker` が無いブラウザでボタンを
  隠していたが、それだと「ボタンが無い」が「ブラウザ未対応」と「古いファイルが
  読まれている」の2通りに読めて切り分けられない。押したときに理由を返す形にした

## v1.8

- コピー文面から絶対パスを外した。添付画像はファイル名と探し方だけを渡す。
  ブラウザは保存先の絶対パスを確実には取得できず、特定のOSと既定のダウンロード先を
  前提にしたパスは環境によって誤りになるため
- 画像の原寸は保存時にローカルへ書き、あわせてIndexedDBにも入れる。後者は
  「書き出し」zip専用（別PCや別の人へ渡すときだけ使う。自動では作らない）
- バーに「保存先」を追加。フォルダを選ぶと以後そこへ無音で書き、まとめて捨てられる。
  選ばなければ従来どおりダウンロードフォルダへ落ちる

## v1.7

- 対応済みパネルの行をクリックすると、状態を変えずに全文と追記を読める
- 対応済みのコメントを保存で書き換えたら自動で未対応へ戻す。コピー文面は未対応しか
  拾わないため、対応済みのまま追記すると保存はされるのにAIへ届かない
- ポップアップが画面下ではみ出す位置では上へ返す

## v1.6

- コメントに追記できる。既存コメントを開くと本文と追記がスレッドで並び、入力欄は
  既定で追記になる。本文そのものを直すときは本文行の「直す」で切り替える。
  データは `comment.replies[]` の追加のみで、追記の無い既存コメントはそのまま読める

## v1.5

- 画像付きコメントがあるとき「コピー」がzipも同時に書き出す動作を入れた（v1.8で撤去）

## v1.4

- 1文字だけの選択でもコメント欄が開く。あわせて、1文字の引用は前後の文脈が外れたときに
  単独一致で別の場所へ付け替えず、位置を見失ったものとして出す
- コメントを書いている間、対象にした範囲を重ねて描く。コメント欄へ文字を打つと
  ブラウザ側の選択ハイライトが消えるため、広い範囲だと対象が見えなくなっていた
- 添付画像の原寸をIndexedDBへ入れられたときはダウンロードしない

## v1.3

- 添付画像の原寸をIndexedDBにもBlobで保管する
- 「書き出し」で未対応コメントの `review.md` と原寸画像を、外部依存なしの無圧縮zipへまとめる

## v1.2

- 出るかどうかは見る側のブラウザが決める。`localStorage` の `rv-layer:enabled` が
  立っているブラウザでしか描画しない。同じHTMLを相手へ送っても相手の画面には出ない
- 表・カード・図など、ブロック単位へのコメント
- コメントへの画像添付（ペースト / ドロップ）
- 選択しただけではコメント欄にフォーカスを移さない（選択したままコピーできる）
