# 変更履歴

まだ公開リリースはしていない。以下は公開前の内部版数の記録。

## v1.11

公開前に、外部モデル4本（Codex 2・Grok 2）で独立レビューを回した結果の反映と、
その過程で見つかった段落破損の修正、配色の入れ替え。

- **保存した位置から復元したコメントが、段落を割ることがあったのを直した**。
  引用が段落の末尾で終わると、レンジの終端が次のブロックの先頭へ落ちて
  境界をまたぎ、`<mark>` が `<p>` ごと飲み込んでいた（元の段落は最初の1文字だけになる）。
  リロードしても直らず、実際にドラッグして再現する。終端の位置を「ひとつ前のノードの末尾」へ
  寄せ、包む直前にも境界を確かめる歯止めを入れた
- **既定の配色をデジタル庁デザインシステムのデザイントークンに入れ替えた**
  （`@digital-go-jp/design-tokens` v2.0.1）。主ボタンは cyan-900、hover は cyan-800。
  文字4.5:1・UI部品の境界3:1（WCAG 2.2 AA）を全項目で満たす。
  以前の配色は主ボタンの白文字が3.28で基準を割っていた。色はすべて `--rv-*` の
  CSSカスタムプロパティで上書きできる
- README に「使う前に知っておくこと」を追加。対象ページのスクリプトから
  コメントと画像が読める・書き換えられること、`window.__rvResolved` を
  ページ側から設定できることを、設計上の前提として明記した
- `package.json` / `SECURITY.md` / `CONTRIBUTING.md` / issueテンプレートを追加
- READMEにスクリーンショットを追加

- 読めない保存データがあるとき、空の状態で上書きしていたのをやめた。この文書のものとして
  読めない保存済みデータを見つけたら、そのセッションは書き込みを止めて残す
- 同じページを2つのタブで開くと、あとから保存した側がもう一方のコメントを消していた。
  保存の直前に最新を読んで取り込む。このタブで削除した分は復活させない
- 画像を複数まとめて投下すると上限4枚をすり抜けていた。読み込み中の枚数も数に入れる。
  読み込み中にコメントが切り替わった画像が、別のコメントへ混入するのも止めた
- 容量超過で保存を止めたあと、容量が空いても再試行しなかった。毎回試して復帰する
- 保存データの形を検査し、壊れた1件で全体が起動不能にならないよう隔離する
- **`__rvResolved` のタグは1つだけ**であることを規約とプロンプトに明記した。
  以前のプロンプトは「複数タグがあってもよい・順に適用される」と書いていたが、
  実装は変数を1回読むだけなので後のタグが前を上書きし、消し込みが黙って落ちていた
- 推奨する `rev` を分から秒までに変えた。同じ分に2回対応すると2回目が丸ごと無視されていた
- `review.md` に出る「追記」を仕様書へ記載した（実装は出していたが仕様書に無かった）
- 済み消し込みのトーストに残っていた内部の呼び名を中立語へ
- READMEの導入例が、対象HTMLからの相対パスであることを明記した
- 手動チェックリストに、保存の守り・`source/`・追記・`xss.html` の確認を足した

## v1.10

- 位置を見失ったコメントの一覧表示で、引用文と指摘文を `innerHTML` へ連結していたのをやめた。
  引用文には元のページの文字列がそのまま入るため、タグに見える文字列がHTMLとして
  解釈される経路になっていた。DOMを組んで入れる形に変更（回帰テストあり）
- 配布物から個人名・内部の課題番号・特定のAIツール名を取り除いた。コピー文面の宛先は
  「AIへ」で、Claude Code / Codex / Cursor / Gemini CLI のいずれでも同じ文面を使える
- 変更履歴をファイル冒頭のコメントからこのファイルへ移した

## v1.9

- 版数を `RV_VERSION` として実体に持たせ、バーのhover・`window.__rv.version`・
  起動時のconsoleの3箇所に出す。読まれているファイルがどの版か分かるようにするため
- 「保存先」ボタンを常時表示に変えた。`showDirectoryPicker` が無いブラウザでボタンを
  隠していたが、それだと「ボタンが無い」が「ブラウザ未対応」と「古いファイルが
  読まれている」の2通りに読めて切り分けられない。押したときに理由を返す形にした

## v1.8

- コピー文面から絶対パスを外した。添付画像はファイル名と探し方だけを渡す。
  ブラウザは保存先の絶対パスを確実には取得できず、特定のOSと既定のダウンロード先を
  前提にしたパスは環境によって誤りになるため
- 画像の原寸は保存時にローカルへ書き、あわせてIndexedDBにも入れる。後者は
  「書き出し」zip専用（別PCや別の人へ渡すときだけ使う。自動では作らない）
- バーに「保存先」を追加。フォルダを選ぶと以後そこへ無音で書き、まとめて捨てられる。
  選ばなければ従来どおりダウンロードフォルダへ落ちる

## v1.7

- 対応済みパネルの行をクリックすると、状態を変えずに全文と追記を読める
- 対応済みのコメントを保存で書き換えたら自動で未対応へ戻す。コピー文面は未対応しか
  拾わないため、対応済みのまま追記すると保存はされるのにAIへ届かない
- ポップアップが画面下ではみ出す位置では上へ返す

## v1.6

- コメントに追記できる。既存コメントを開くと本文と追記がスレッドで並び、入力欄は
  既定で追記になる。本文そのものを直すときは本文行の「直す」で切り替える。
  データは `comment.replies[]` の追加のみで、追記の無い既存コメントはそのまま読める

## v1.5

- 画像付きコメントがあるとき「コピー」がzipも同時に書き出す動作を入れた（v1.8で撤去）

## v1.4

- 1文字だけの選択でもコメント欄が開く。あわせて、1文字の引用は前後の文脈が外れたときに
  単独一致で別の場所へ付け替えず、位置を見失ったものとして出す
- コメントを書いている間、対象にした範囲を重ねて描く。コメント欄へ文字を打つと
  ブラウザ側の選択ハイライトが消えるため、広い範囲だと対象が見えなくなっていた
- 添付画像の原寸をIndexedDBへ入れられたときはダウンロードしない

## v1.3

- 添付画像の原寸をIndexedDBにもBlobで保管する
- 「書き出し」で未対応コメントの `review.md` と原寸画像を、外部依存なしの無圧縮zipへまとめる

## v1.2

- 出るかどうかは見る側のブラウザが決める。`localStorage` の `rv-layer:enabled` が
  立っているブラウザでしか描画しない。同じHTMLを相手へ送っても相手の画面には出ない
- 表・カード・図など、ブロック単位へのコメント
- コメントへの画像添付（ペースト / ドロップ）
- 選択しただけではコメント欄にフォーカスを移さない（選択したままコピーできる）
