# セキュリティ

## 先に読んでほしい設計上の前提

`rv-layer.js` は対象HTMLと**同じJavaScriptの権限で動く**。分離された領域は無い。
そのため次のことは脆弱性ではなく、この作り方の帰結として最初からそうなっている。

- 対象ページの他のスクリプトが、コメント本文と添付画像を読める・書き換えられる
  （保存先は同一オリジンの `localStorage` と `IndexedDB`）
- ページ側のスクリプトが `window.__rvResolved` を設定して、実際には直っていない
  コメントを「済み」へ落とせる

詳しくは README の「使う前に知っておくこと」を参照。**中身を把握しているページで使う**
ことを前提にしている。これらの報告は受け取っても修正できないので、報告の前に一度確認してほしい。

## 報告してほしいもの

上の前提を超えて起きること。たとえば:

- 対象ページの本文に含まれる文字列が、レビュー層のUIで**HTMLとして解釈される**経路
  （引用文・コメント本文・画像のファイル名・ページタイトルなど）
- 書き出したzipの中身から、展開時に意図しない場所へファイルが書かれる経路
- コメントや画像が、操作していないのに失われる・別のコメントへ移る経路
- 保存したデータが、別の文書のものと取り違えられる経路

## 報告の仕方

**公開のissueを立てないでほしい。**
GitHubの [Security Advisories](https://github.com/kevin-slvc/akaire/security/advisories/new)
から非公開で報告してください。

含めてほしいもの: 再現するHTML（最小のもの）、使ったブラウザとバージョン、
起きたことと期待した動作。

個人で管理しているため、応答時間の約束はできません。受け取ったら返事はします。

## 対象バージョン

最新の1つだけをみる。古いバージョンへのバックポートはしない。
